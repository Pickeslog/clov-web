# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets / env (never commit)
# Vite 기본 .gitignore에는 .env 규칙이 자체가 없다 — `*.local`이 `.env.local`만 우연히 걸러줄 뿐,
# `.env`·`.env.production`은 그대로 커밋된다. 배포 설정(VITE_API_BASE_URL 등)이 여기 들어가므로 막는다.
# clov-api에서 같은 종류의 구멍으로 실제 시크릿이 커밋에 딸려 들어간 적이 있다(2026-07-27).
**/*.env
**/.env.*
!**/*.env.example
!**/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
